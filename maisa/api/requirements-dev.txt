# Dependencias SOLO para desarrollo y tests. No entran en la imagen Docker.
#   uv pip install --python .venv-api/bin/python -r requirements.txt -r requirements-dev.txt
#
# httpx ya viene en requirements.txt (el proxy del OCR lo usa), pero se repite
# aqui porque el TestClient de FastAPI tambien lo necesita.
# 9.0.3 cierra GHSA-6w46-j5rx-g56g (el reporter de fallos podia leer ficheros
# fuera del directorio de trabajo con `--junitxml` sobre un path controlado).
pytest==9.0.3
httpx==0.28.1
